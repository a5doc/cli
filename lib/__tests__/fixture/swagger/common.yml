---
swagger: "2.0"
info:
  version: "COMMON-VER"
  title: "swagger test"
schemes:
- "https"
# common.ymlのpathsはマージされません
paths:
  /dummy:
    get:
      operationId: "dummy"
      responses:
        200:
          description: "dummy"
definitions:
  Order:
    type: "object"
    properties:
      id:
        type: "integer"
        format: "int64"
      petId:
        type: "integer"
        format: "int64"
